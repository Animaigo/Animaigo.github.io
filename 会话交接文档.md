# 会话交接文档

> 交接日期：2026-09-22
> 给下一个没有任何上下文的 Codex 会话阅读。详细改动历史见同目录的 `博客改动总结.md`（已写到第 40 条）。

## 一、项目与常用命令

- 项目目录：`C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog`
- 包名：`mikan-fuyukawa-blog`；技术栈：Astro 6 静态站 + Pagefind + TypeScript
- 原 Yuimi 仓库（只读参考/素材源）：`D:\PrivateBlog\yuimichaya`
- 本地预览：`http://127.0.0.1:4322/`（当前这个 dev 进程会随对话/应用关闭而消失）

```bash
npm run dev      # 本地开发（常用端口 4322）
npm run build    # 资源生成 + Astro 构建 + 性能预算检查
npm test         # 静态测试（当前 16/16 通过）
```

**必须知道的坑：**

1. 在 Codex 沙箱里 `npm run dev` / `npm run build` 会因 esbuild 需要完整文件系统而失败，**必须用 escalation 运行**（已批准的 prefix rule：`npm run dev`、`npm run build`）。
2. **这个项目的 `astro dev` 改完 `.astro` / `theme.css` 后不会自动刷新**：服务端渲染时内联的 CSS 与页面模块仍是旧快照（浏览器刷新也没用），**必须停掉再重启 dev 服务**。`dist/` 构建产物始终是最新的。
3. 项目目前**没有 `.git`**，任何删除都不可通过版本控制恢复；删东西前先备份。
4. **Astro scoped 样式的坑（第 39 条踩过）**：模板里的元素会被加上 `data-astro-cid-*`，而 `document.createElement` 生成的节点没有这个属性，写在同一页 `<style>` 里的选择器**匹配不上 JS 生成的 DOM**（阅读条横条、卡片详情都栽在这上面：整串进度条不可见、详情面板等于没样式）。这类选择器必须写成 `:global(...)`，包括 `> *` 这种通配子项。
5. **验证工具（别再只靠静态断言）**：`tmp/verify-ui.mjs`（无头 Chrome + CDP，量阅读条 / 卡片 / 胶囊并截图到 `tmp/*.png`）、`tmp/verify-pill.mjs`（派发真实鼠标事件复现「点两下收起」）。两个脚本都要 escalation 跑。Chrome 的 `--user-data-dir` 必须放系统临时目录——放进项目 `tmp/` 会让 dev 的文件监听 EBUSY 崩掉（已踩过一次）。
- 改完 `.astro` / `theme.css` 必须重启 dev 才能看到新版（见上条 2）；`dist/` 是最新的，可以先用 `dist/` 或静态检查确认。

## 二、这一阶段（2026-09-13 → 09-21）做了什么

按主题概述，细节与验证记录看 `博客改动总结.md` 对应条目：

- **文案对齐现状**：Blank 首页"不加载…"文案改成主题壁纸/雪花/Fuyukawa Kagari；修复右键菜单仍显示"樱花雨"的 bug（静态 HTML 是雪花、被 JS 覆盖）；blog 搜索框占位符去掉旧站示例关键词（`AstrBot、插件、部署` → `输入关键词…`）；README 同步。
- **头像与社交链接**：全站头像换成 `C:\Users\luosh\Pictures\Tx1.jpg` 生成的 `profile.webp`（1080×1080）；`profile.ts` 成为社交链接唯一数据源（github / bilibili / bangumi），首页英雄区、Me 页、Blank About 三处共用，留空即隐藏。
- **动效**：按压反馈（`scale(.97)`）、搜索结果"空→有"入场、切歌标题淡入、暗门看板娘召唤；顶部导航在下半区铺满后常驻；英雄区光标改成不闪的老式下划线 + 打字提速到 68ms；Anime 标题做成共享元素过渡（Me 页文字 → Anime 页标题）。
- **返回按钮**：只在"有真实父级"的页面出现——文章页 → `/blog/`、`/anime/` → `/about/`；其它一级页面（blog 列表、projects、about、games、404、首页）都没有。行为是"优先站内历史回退，否则跳父级"。
- **Bangumi 追番**：新增 `scripts/sync-bangumi.mjs`（v0 分页 → 旧接口回退，可 `--from-file` 离线解析、`--no-covers`、`--dry-run`），数据落在 `src/core/data/bangumi.json`（当前 uid `1028220`、**203 部**），封面下载到 `public/bangumi/`。Me 页只留一个"Anime"链接入口，完整列表在 `/anime/`（两个主题都是按状态分组、条目用 `·` 分隔的流水清单；Fuyukawa 的"在看"带封面卡）。
- **友链**：`src/core/data/friends.ts`（唯一数据源），首条是 **Elysia**（`https://seele.wiki`，头像已按源站居中裁切存为 `public/friends/seele-wiki.webp`）；Fuyukawa Me 页底部卡片网格 + Blank About 行卡。
- **深色模式**：`src/core/ColorMode.astro`（`<head>` 内联脚本 + `data-astro-rerun`，渲染前生效不闪白），偏好键 `mikan-color-mode-v1`（`auto|light|dark`，默认跟随系统），`html[data-color-mode]` 驱动；切换时用 480ms View Transition 交叉淡入。暗色皮肤是"token 重定义 + 精选覆盖"（见 `theme.css` 末尾的深色块），**图片类 background 在两种模式下完全一致，变暗只靠独立遮罩层**（避免切换位移）。
- **右下角快捷模块**：`src/core/QuickActions.astro`（两个 layout 共用）。**Fuyukawa 侧已改成胶囊**：把手 + 月亮 + 调色板共用一层 `.quick-actions::before` 胶囊底，收起时用 `clip-path: inset(0 0 0 calc(100% - 52px) round 999px)` 只罩住把手、展开时放开裁剪（220ms `--ease-out`，不动宽高），三个按钮是胶囊内的透明图标槽。交互不变：收起只露把手（⋯），hover/聚焦向左展开；**把手在桌面和触屏都是开关**——点一下展开并保持（`is-pinned`），再点一下收起（同时加 `is-collapsed` 抑制态压过 `:hover`，指针离开模块时清掉）；鼠标移开模块即收（`pointerout` 只在 `pointerType === "mouse"` 时取消钉住）；触屏点过菜单按钮后自动收起。**Blank 主题的快捷模块还是原来的三个圆钮，未改。**
- **页脚访问统计**：原来的旅行者一号/二号换成"本站总访问 / 今天你是第几位来访者"，客户端请求第三方 `abacus.jasoncameron.dev`（两个 key：`total`、`day-YYYY-MM-DD`，每会话只计一次，localStorage 缓存，本地预览用独立命名空间 `mikan-blog-dev-probe`）。
- **两个预览页（功能已合并，页面本身暂留）**：`/preview/reader/` 与 `/preview/projects/` 现在和正式页共用同一份代码——阅读条组件 `src/core/ReadingRail.astro`（一个标题一条小横条：H1 23 / H2 17 / H3 11px、厚 3px、当前章节近黑其余浅灰；悬停只浮出文字标题、没有竖条和背景框；位置 `article.left - 48`）已经接进 `ArticleLayout`，项目拼贴 + 详情面板已经接进 `ProjectsPage`。两个预览页带 `noindex,nofollow` 且被 sitemap 排除，等用户确认后删除。

## 三、当前状态与关键机制

- 路由：Fuyukawa 主题占根路由（`/`、`/blog/`、`/blog/<slug>/`、`/projects/`、`/about/`、`/anime/`、`/games/`、`/404`、`/preview/...`）；Blank 在 `/themes/blank/...`。
- 数据源：`src/core/data/` 下 `profile.ts`（身份+社交链接）、`friends.ts`、`bangumi.ts`/`bangumi.json`、`projects.ts`（**仍是空数组**）、`games.ts`。
- 主题注册与路径：`src/core/themes/registry.ts`（`getCanonicalPath` / `getThemePath`，默认主题 `fuyukawa-kagari`）。
- 状态键：主题 `yuimi-theme-id-v2`、暗色 `mikan-color-mode-v1`、看板娘 `mikan-live2d-enabled-v1`、雪花开关 `sakuraStateKey`（内部标识符保持 `yuimi-*` / `sakura*` 不动）。
- 质量门：`npm test` 16/16（静态断言）；`npm run build` 16 页 + 性能预算（Home CSS 约 122 KiB / 上限 312.5 KiB）。
- sitemap 过滤：排除 `/themes/` 与 `/preview/`。

## 四、待办（建议按此优先级）

1. **两个预览页的功能已经在第 40 条合并进正式页**：
   - 阅读条 = 共用组件 `src/core/ReadingRail.astro`（标记 + 脚本）+ `theme.css` 里的全局样式；`ArticleLayout.astro` 已经接上（`data-reader-article`），静态 `ON THIS PAGE` 目录已撤，正文单栏居中 860。
   - 项目拼贴 + 详情面板已经进 `ProjectsPage.astro`，数据在 `projects.ts`（**里面 6 条是示例数据，等用户给真实条目**）。
   - **剩下要做的**：等用户确认正式页观感后，删掉 `/preview/reader/`、`/preview/projects/` 两个页面、`astro.config.mjs` 里的 `/preview/` sitemap 过滤、`BaseLayout` 的 `noindex` 属性，以及 `theme.css` 里已经没人用的 `.article-toc*` 与 `.works-*` 旧样式。
2. **Bangumi 封面升级**：现有 16 张"在看"封面取自 `grid` 缩略图（100px，显示偏糊）。脚本已改为优先 800px 源、输出 480px，让用户在本机跑：

   ```powershell
   $env:HTTPS_PROXY="http://127.0.0.1:7890"
   node --use-env-proxy "C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\scripts\sync-bangumi.mjs" --from-file="C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\tmp\bangumi-raw.json" --uid=1028220
   ```

3. **Bangumi 图标**：现装图标库（simple-icons / tabler / arcticons / CoreUI / logos / IconPark）都没有 Bangumi 官方标志，npm 上的开源插件包也只有截图；目前英雄区用的是通用外链图标。等用户把 logo 图片放到 `C:\Users\luosh\Pictures` 后接成本地资源，或改用"bgm"文字徽标。
4. **访问统计可达性**：`abacus.jasoncameron.dev` 的连通性无法在沙箱验证。若页脚显示 `—`，改用不蒜子（只有总访问量/访客数）或 Cloudflare Worker + KV（两个数字都有）。
5. **部署（一直没做）**：项目无 `.git`，`astro.config.mjs` 的 `site` 仍是原 Yuimi 地址 `https://yuimi-chaya.github.io`；需要用户确认目标仓库与托管方式（个人页/项目页决定是否要 `base`），再初始化 git、改 `site`/`base`、部署。
6. **版权/隐私（公开前必须处理）**：`MUSIC/`（9 首，根目录是 manifest 来源）与 `public/themes/fuyukawa-kagari/music/` 仍是原 Yuimi 的动漫歌曲；首页天气/定位仍在调第三方接口且带旧的 Tencent 地图 key（见 `HomePage.astro` 的 `tencentMapKey`）；Live2D 暗门启用后仍会请求 jsdelivr 资源。
7. **暗色细节收尾**：暗色是精选覆盖，个别装饰（贴纸/胶带、部分半透明渐变）可能还偏亮，发现即补。
8. **404 页面**：用户已确认**保留**（GitHub Pages 用根目录 `404.html` 兜底，且它承载 `/themes/blank/*` 的重定向脚本），不要再提议删除。

## 五、用户明确要求过的硬约束

1. 图像不得由 AI 自行创作；只有用户明确要求生成时才可生成，优先使用用户提供/原始素材。
2. Kisara 主题保持删除，不要恢复为默认主题。
3. Blog 页搜索框不要隐藏、不要改结构（占位符文案已按用户要求改为"输入关键词…"）。
4. 不要重命名 `yuimi-*` 之类的内部标识符（存储键、事件名）；只改用户可见文案。
5. 用户会说 **"grill me"**：意思是"不确定就先问，别自己猜"。涉及范围、位置、行为、审美方向的判断，**先按 grilling 技能问一轮（每个问题给出推荐答案）再动手**；这一条被用户反复强调过，违反会挨骂。
6. 视觉细节（动画手感、裁切位置、深浅色观感）必须由用户目视验收；Codex 侧只做结构与数值验证。

## 六、常用文件索引

- 主题路由注册 / 路径工具：`src/core/themes/registry.ts`
- 站点文字/导航：`src/lib/site.ts`
- 个人资料与社交链接（唯一数据源）：`src/core/data/profile.ts`
- 友链数据：`src/core/data/friends.ts`
- Bangumi 数据与类型：`src/core/data/bangumi.ts`、`src/core/data/bangumi.json`（同步脚本 `scripts/sync-bangumi.mjs`，命令 `npm run sync:bangumi -- <uid>`）
- 项目/作品数据（空）：`src/core/data/projects.ts`
- 公共布局（页头/页脚/工具抽屉/暗色/快捷模块）：`src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`、`src/core/ColorMode.astro`、`src/core/QuickActions.astro`
- 返回按钮：`src/core/navigation/BackButton.astro`、`src/core/navigation/backTarget.ts`
- 首页：`src/themes/fuyukawa-kagari/pages/HomePage.astro`
- Me 页：`src/themes/fuyukawa-kagari/pages/AboutPage.astro`（Blank 对应 `src/themes/blank/pages/AboutPage.astro`）
- Anime 页：`src/themes/fuyukawa-kagari/pages/AnimePage.astro`、`src/themes/blank/pages/AnimePage.astro`
- 文章布局（含现有 TOC）：`src/themes/fuyukawa-kagari/layouts/ArticleLayout.astro`
- Projects 页：`src/themes/fuyukawa-kagari/pages/ProjectsPage.astro`
- 预览页（未合并）：`src/pages/preview/reader.astro`、`src/pages/preview/projects.astro`
- 样式总入口：`src/themes/fuyukawa-kagari/styles/theme.css`（深色块在文件末尾）、`src/themes/blank/styles/theme.css`
- 测试：`tests/*.test.ts`

## 七、推荐下一步

1. 先问用户两个预览页看得怎么样（进度栏形态 / 卡片放大手感），按反馈微调。
2. 让他提供真实项目数据（标题 / 简介 / 封面 / 链接 / 技术栈），然后合并阅读栏与项目卡片，删掉预览页。
3. 顺手让他跑一次 Bangumi 封面升级命令。
4. 之后就是部署与版权清理两条主线（都要先问清目标仓库与功能去留）。
