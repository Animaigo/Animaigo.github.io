# 博客改动总结

> 项目：`C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog`
> 更新时间：2026-09-06
> 下面每条“改动”都说明具体行为、涉及文件，以及以后想改/恢复时的入口。

## 1. 隐藏看板娘、设置开关

具体行为：

- Live2D 控制台默认 hidden，全站（首页、Blog、文章、Games、Projects、About）都不显示。
- 看板娘初始化逻辑保留但默认不启动，不会自动请求外部资源。
- 暗门已生效：浏览器控制台输入 `mikanShowDoll()` 会恢复看板娘和 Live2D 控制台；输入 `mikanHideDoll()` 会再次隐藏。偏好存在 localStorage，刷新或跳转后仍保持。
- Mikan Radio 和 TOOLS 抽屉原样保留，播放器功能不受影响。

改动位置：

- `src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`
  - `.live2d-control-widget` 默认带 `hidden`。
  - 状态键：`mikan-live2d-enabled-v1`。
  - 暗门函数：`window.mikanShowDoll()` / `window.mikanHideDoll()`。
- `src/themes/fuyukawa-kagari/styles/theme.css`：`.live2d-control-widget[hidden] { display: none !important; }`

恢复/修改入口：删除代码中的隐藏逻辑即可重新默认显示；想让暗门命令改名则改 `BaseLayout.astro` 里 `window.mikanShowDoll` 的赋值。

## 2. 从原仓库复制新项目，删除 Kisara

具体行为：

- 新项目基于 `D:\PrivateBlog\yuimichaya` 复制，不携带 `node_modules`。
- Kisara 已删除：源码目录、路由页面、公共图片资源、Kisara 测试、Kisara 制作脚本和 README 演示图。
- 保留 Fuyukawa Kagari 和 Blank 两套主题；Fuyukawa Kagari 作为默认主题，直接承载根路由。
- Blank 保留在 `/themes/blank/`。
- 全站 12 个页面（Fuyukawa 5 个主路由 + Blank 对应路由 + 404 等）。

改动位置：

- `src/core/themes/registry.ts`：默认主题改为 `fuyukawa-kagari`，主题列表只剩 Fuyukawa + Blank。
- `src/pages/`：根路由页面全部改为引入 Fuyukawa 页面。
- `scripts/check-performance-budgets.mjs`、`tests/`：同步改为检查 Fuyukawa/Blank。
- `edgeone.json`、`preview-blog.bat`、`README.md`：同步清理 Kisara 引用。

恢复/修改入口：不要把原 Kisara 直接拷回来作为默认主题；如需参考原站行为，看 `D:\PrivateBlog\yuimichaya`。

## 3. 站点身份与文字改成 Mikan KenkyuuSho / Reisen⁴

具体行为：

- 站点名、标题、作者、页脚、SEO、浏览器标题改为 Mikan KenkyuuSho / Reisen⁴。
- 首页英雄区标题、终端打字副标题、About、Blog、Games、Projects、404、Blank 页面的标题与描述同步替换。
- 原 Yuimi 文章已移除，新增一篇 `hello-mikan.md` 草稿（draft，不发布）。
- GitHub 链接改为 `https://github.com/Animaigo`；Bilibili 链接改为用户提供的空间地址（Hero 处只显示图标）。

改动位置：

- `src/lib/site.ts`：站点配置与导航（GAME 已在导航中移除）。
- `src/core/data/profile.ts`：个人资料与数组数据（技术线/动漫/游戏等改为空数组+注释，等待填写）。
- `src/core/content`、`src/themes/.../pages`：页面标题/描述。
- `src/content/blog/`：只剩草稿。

恢复/修改入口：所有可替换文字优先在 `site.ts`、`profile.ts`、各页面 title/description 修改；不要直接批量替换内部标识符（如 `yuimi-*` 存储键/事件名）。

## 4. Hero：移除头像花饰，终端框改为白亮打字屏幕

具体行为：

- 头像上方的红花形装饰已移除。
- 头像右侧的终端框改为“发白光的荧幕”：无深色终端装饰、无路径、无 terminal 圆点。
- 打字内容（三行）：
  - `Reisen⁴`
  - `或许我们的日常，`
  - `是由一串串奇迹连结而成的呢。`
- 中英文当前按同样约 83ms/字符打字；名字行字号明显大于简介行。
- 简介打完后，GitHub / Bilibili 两个纯图标按钮淡入（不再显示文字）。

改动位置：

- `src/themes/fuyukawa-kagari/pages/HomePage.astro`：身份区结构和打字脚本。
- `src/themes/fuyukawa-kagari/styles/theme.css`：`.identity-terminal`、`.identity-screen`、`.identity-line--name`、`.identity-links`。

恢复/修改入口：

- 想恢复红花：恢复 HomePage 中 `.avatar-flower` 相关 markup（原版在 `D:\PrivateBlog\yuimichaya`）。
- 想改打字内容/速度：改 HomePage 里 `profileLines` 和 `profileCharDelay`。
- 想改名字字号：改 `.identity-line--name` 的 `font-size`。

## 5. Hero 白膜/渐变降低

具体行为：

- 覆盖在 sz01 图片上的淡白膜从 8% 降到约 2%。
- Hero 顶部与底部的白色渐变明显减淡，底部不再接近实白，让图片更接近原色。

改动位置：`theme.css` 的 `.hero-stage`、`.hero`、`.hero::after`、`.hero-overlay`。

## 6. 顶部导航：隐藏 GAME、去掉下拉半圆矩形、保留中央胶囊

具体行为：

- 顶部导航不再显示 GAME；`/games/` 页面仍保留可访问。
- 隐藏的大容器下方那个“半圆角矩形”已删除（`.site-header::after { content: none; }`）。
- 保留中央“内外两圈”的小胶囊（`.site-header::before`）。
- 大容器的隐藏/下拉机制未改动。

改动位置：

- `src/lib/site.ts`：移除 `games` 导航项。
- `theme.css`：`.site-header::after`。

恢复入口：想恢复 GAME 导航就在 `site.ts` 的 `nav` 数组加回 `games` 项；想恢复下拉把手就重新启用 `.site-header::after` 样式（可从 `D:\PrivateBlog\yuimichaya` 原 CSS 找回）。

## 7. 今日公告按钮

具体行为：

- “今日公告”文字删除，按钮只保留 `💌`，仍可点击打开公告弹窗。
- 按钮背景改为透明毛玻璃（淡白 + 描边 + 高斯模糊）。
- 公告弹窗内容未改。

改动位置：`HomePage.astro` 的 `.home-notice-trigger` 与 `theme.css`。

## 8. 首页两个底窗（时间 / “你终于回来了”）改为玻璃窗口

具体行为：

- 时间窗口与欢迎窗口保留半透明玻璃外观：淡白底色、细描边、圆角、2px 高斯模糊。
- 不是实心面板，后面画面可以透出。

改动位置：`theme.css` 的 `.home-signal-card`、`.home-welcome-bubble`。

恢复/修改入口：修改这两段的 `background`、`backdrop-filter`、`border` 即可。

## 9. 樱花雨改成下雪（全站）

具体行为：

- 全站固定层由樱花花瓣改为白色圆形雪团。
- 34 片雪团仍各有不同大小、透明度、下落时长与左右摇摆幅度。
- 右键菜单和页脚文案已改为“雪花 / snow”。

改动位置：

- `theme.css`：`.sakura-rain span`、`@keyframes snow-fall`、`@keyframes snow-sway`。
- `BaseLayout.astro`：右键菜单按钮与页脚状态文案。

恢复入口：把 `.sakura-rain span` 外观与动画名改回樱花版本（原版见 `D:\PrivateBlog\yuimichaya`）。

## 10. 用户图片替换与压缩

具体行为：

- 首页上半 Hero 图：`C:\Users\luosh\Pictures\sz01.png` → `public/themes/fuyukawa-kagari/assets/hero-wallpaper.webp`。
- 下半部分背景图：`C:\Users\luosh\Pictures\sz021.jpg` → `public/themes/fuyukawa-kagari/assets/fuyukawa-kagari-bg.webp`。
- 均压缩为 WebP、1920px 尺寸：
  - hero-wallpaper.webp：1920×1080，约 650KB。
  - fuyukawa-kagari-bg.webp：1920×1360，约 113KB。
- 上半图按“贴底边”裁切；下半图按“贴顶边”裁切。

改动位置：`public/themes/fuyukawa-kagari/assets/` 两个 `.webp`；`theme.css` 中两处 `background-position` 已分别改成 `center bottom` / `center top`。

恢复/修改入口：直接替换同名 `.webp` 即可；需要重新裁切时，建议用 sharp（项目内有 `sharp`）处理为相同尺寸。

## 11. 验证命令

```bash
npm run dev        # 本地开发（当前常用端口 4322）
npm run build      # 构建 dist/，并检查性能预算
npm test           # 静态测试（当前 10/10 通过）
```

## 12. 图片二次替换（2026-09-06，用户确认后执行）

具体行为：

- 首页上半 Hero 图由 sz01 换成 `C:\Users\luosh\Pictures\sz03.jpg`：压缩裁切为 1920×1080 WebP（保底边，约 274KB），覆盖同名 `hero-wallpaper.webp`。
- 下半背景图由 sz021 换成 `C:\Users\luosh\Pictures\sz4.png`：源为带全不透明 alpha 通道的 PNG，先去掉 alpha，再放大压缩为 1920×1360 WebP（保顶边，约 90KB），覆盖同名 `fuyukawa-kagari-bg.webp`。
- 图片比例说明：sz03.jpg 为 3095×1941，按 16:9 目标裁掉约 10% 顶部；sz4.png 为 1286×909，比例与 1920×1360 几乎一致，属放大使用。
- 验证：`npm test` 10/10 通过；`npm run build` 12 页构建成功、Pagefind 12 页、性能预算全部通过；本地预览 http://127.0.0.1:4322/ 供目视验收。

改动位置：`public/themes/fuyukawa-kagari/assets/` 下两个 `.webp`。

恢复/修改入口：旧图仍可由 `C:\Users\luosh\Pictures\sz01.png`、`sz021.jpg` 重建；不满意可直接再换源图执行同样裁切。

## 13. 下半图对齐底边 + 白膜对齐上半（2026-09-06）

具体行为：

- 下半主背景 `.home-content-bg` 与页脚 `.site-footer` 里的 `fuyukawa-kagari-bg.webp` 背景锚点由 `center top` 改为 `center bottom`（沿底边放图）。
- 下半白膜数值对齐上半 `.hero` 同款分层：`.home-content-bg` 底色渐变改为 `0.08 → 0.04(44%) → 0.12`；`::before` 改为左右 `0.07 / 0.09`、纵向 `0.04 → 透明(58%) → 0.45`（与 `.hero::after` 一致）；`.tag-rain-stage` 的白色渐变底部从 `0.2` 降到 `0.06`，避免下半中部过白。
- 页脚自身较厚的纸面渐变（0.44-0.72）未动，避免影响页脚文字可读性。

改动位置：`src/themes/fuyukawa-kagari/styles/theme.css`。

验证：`npm test` 10/10 通过；`npm run build` 12 页构建成功、性能预算全部通过；预览 http://127.0.0.1:4322/ 供目视验收。

## 14. 首页下半区铺满后导航常驻（2026-09-13）

具体行为：

- 首页下半背景 `.home-content-bg` 完全铺满视口（`getBoundingClientRect().top <= 0`）后，顶部导航从悬停下拉变为常驻：`.site-header` 加上 `is-nav-pinned`，复用原有 260ms `top` 过渡滑下；滚回英雄区（下半区 top > 0）时自动收回，恢复"悬停/聚焦才展开"的原行为。
- 只在首页生效：blog / works / me 等内容页不加这个类，保持原先的悬停下拉。
- 判定用 rAF 节流的被动 scroll/resize/pageshow 监听，并在 `__yuimiHeroCleanup` 清理函数里移除监听、取消帧请求、摘掉 `is-nav-pinned`。

改动位置：

- `src/themes/fuyukawa-kagari/pages/HomePage.astro`（`syncNavPin` / `scheduleNavPin`）
- `src/themes/fuyukawa-kagari/styles/theme.css`（`.site-header.is-nav-pinned`）
- `tests/fuyukawa-performance.test.ts`（新增静态断言）

验证：`npm test` 11/11 通过；`npm run build` 12 页构建成功、性能预算全部通过；预览 http://127.0.0.1:4322/ 供目视验收。

## 15. 非首页背景改为页脚同款透明度（2026-09-13）

具体行为：

- 新增 `BaseLayout` 的 `surface` 属性（`"cover" | "page"`，默认 `"page"`），输出 `<body class="has-cover-surface">` 或 `<body class="has-page-surface">`；首页 `HomePage.astro` 显式传 `surface="cover"`，其余页面（blog / projects / about / games / 404 / 文章页）自动落到 `has-page-surface`。
- `body.has-page-surface` 使用与 `.site-footer` 完全相同的背景栈：粉/蓝光斑 `0.38 / 0.34`、纸白渐变 `0.44 → 0.72(42%) → 0.58`、`fuyukawa-kagari-bg.webp` `center bottom / cover no-repeat fixed`。原先 `body` 的浅黄渐变（`#fff8ec → #f8efe3`）与 `0.8` 强光斑只保留给首页封面层。
- 整页背景不再有整块不透明浅黄层，底图按页脚透明度透出；页面内容仍各自有玻璃卡片垫底。

改动位置：

- `src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`
- `src/themes/fuyukawa-kagari/pages/HomePage.astro`
- `src/themes/fuyukawa-kagari/styles/theme.css`
- `tests/fuyukawa-performance.test.ts`（新增静态断言）

验证：`npm test` 12/12 通过；`npm run build` 12 页构建成功、性能预算全部通过；dist 产物确认首页为 `has-cover-surface`、其余页为 `has-page-surface`。可读性仍需用户目视验收（blog 时间线左侧日期直接压在底图上，是全页唯一没有卡片垫底的文字）。

## 16. Me 页移除 Now status 与 life x diary（2026-09-13）

具体行为：

- 删除侧栏 "Now status" 卡片（`.about-status-card`）与其列表样式，`AboutPage.astro` 不再引用 `profileStatus`/`statusItems`。
- 删除 README 区右上角的 "life x diary" 圆形徽章（`.about-readme-badge`）及其样式；`.about-readme-hero` 由「正文 + 136px 徽章」两列改为单列，避免留下空档。
- `profileStatus` 数据本身保留在 `src/core/data/profile.ts`（Blank 主题仍在读取）。

改动位置：`src/themes/fuyukawa-kagari/pages/AboutPage.astro`。

验证：`npm test` 12/12 通过；`npm run build` 12 页构建成功、性能预算全部通过（About HTML 32.1→30.4 KiB，CSS 106.0→104.3 KiB）；dev 服务重启后确认 `/about/` 不再输出这两块。

## 17. 重要：Astro dev 需要重启才会刷新

本项目的 `astro dev` 存在一个反复出现的现象：修改 `.astro` 或 `theme.css` 后，客户端模块会更新，但**服务端渲染页面里内联的 CSS 与组件模块仍是旧快照**，浏览器刷新也看不到变化。`npm run build` 的 `dist/` 产物始终是最新的。

结论：每次改完样式或页面，若要给用户看预览，必须停掉再重启 `npm run dev -- --host 127.0.0.1 --port 4322`，不要依赖 HMR。

## 18. 头像换成 Tx1 + 社交链接单一来源 + 四条动效（2026-09-13）

具体行为：

- 头像：`public/themes/fuyukawa-kagari/assets/profile.webp` 由 `C:\Users\luosh\Pictures\Tx1.jpg`（1080×1080）重新生成（1080×1080 WebP，59KB，比旧图 83KB 更小）。首页英雄区头像、Me 页头像卡、看板娘本地回退头像、SEO 分享图都引用同一文件，因此一次性全部更新；旧图可从 `D:\PrivateBlog\yuimichaya\public\themes\fuyukawa-kagari\assets\profile.webp` 恢复。
- 链接单一来源：`src/core/data/profile.ts` 的 `profileIdentity.github` / `profileIdentity.bilibili` 现在是首页英雄区与 Me 页 Profile README 共用的唯一数据源，留空即隐藏。补上 `bilibili: "https://space.bilibili.com/23345964"`（去掉原来的 `spm_id_from` 跟踪参数）；英雄区改为读同一份数据，B 站链接为空时用 `hidden` 隐藏（配套 `.identity-links a[hidden] { display: none; }`）。
- 动效一（按压反馈）：`.music-controls button` / `.live2d-controls button` / `.custom-context-menu button` / `.kanban-action` / `.home-notice-trigger` / `.theme-quick-switch` / `.toy-dock-handle` 增加 `:active { transform: … scale(0.97) }`，transform 过渡统一为 `160ms var(--ease-out)`。
- 动效二（搜索结果入场）：`.blog-search-results.is-entering .blog-search-result` 播放 `search-result-in 160ms var(--ease-out)`，2–5 项依次延迟 `20/40/60/80ms`；`BlogIndexPage.astro` 的 `setSearchResults()` 只在「空 → 有结果」时加类，连续输入不会反复闪。
- 动效三（切歌标题）：`.music-track` 增加 `140ms var(--ease-out)` 的 opacity/transform 过渡，`updateMusicUi()` 换标题时先加 `is-swapping` 再更新文本，标题不再瞬变。
- 动效四（暗门召唤）：`#waifu.is-summoned` 播放 `doll-summon 320ms var(--ease-pop)`（用独立 `translate`/`scale` 属性，避免覆盖第三方库的 `transform`），调用点在看板娘显示的两处；`summonWaifu()` 负责重放动画。
- 降级：新增共享 token `--ease-out: cubic-bezier(0.23, 1, 0.32, 1)` 与 `--ease-pop: cubic-bezier(0.18, 1.08, 0.25, 1)`；`prefers-reduced-motion: reduce` 下按压反馈改为只变暗（`filter: brightness(0.96)`）、搜索入场改为纯淡入、切歌与召唤动画关闭；lite 模式下召唤动画也关闭。

改动位置：`src/core/data/profile.ts`、`src/themes/fuyukawa-kagari/pages/HomePage.astro`、`src/themes/fuyukawa-kagari/pages/BlogIndexPage.astro`、`src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`、`src/themes/fuyukawa-kagari/styles/theme.css`、`public/themes/fuyukawa-kagari/assets/profile.webp`。

验证：`npm test` 13/13 通过；`npm run build` 12 页与性能预算全部通过；重启 dev 后确认 About/首页都输出 B 站链接、`profile.webp` 已是新图（59,456 bytes）。

## 19. 英雄区打字光标改为不闪的老式下划线 + 提速（2026-09-13）

具体行为：

- `.identity-screen-cursor`（英雄区可见光标）：由 `9px × 1.12em` 竖条改为 `0.5em × 4px` 水平下划线（长度与厚度按用户指定调整），`vertical-align: -0.06em` 贴在基线下方，保留青色发光；移除 `cursor-blink` 动画，光标常亮不闪。
- `.terminal-cursor`（英雄区隐藏终端行里的备用光标）同步改为 `0.5em × 4px` 下划线样式，并移除闪烁；移动端覆盖中的 `margin-top: 25px` 改为 `0`。
- 删除已无引用的 `@keyframes cursor-blink`。
- 打字速度：`profileCharDelay` 由 `83ms` 改为 `68ms`（约快 18%），行间停顿与链接淡入节奏保持不变。

改动位置：`src/themes/fuyukawa-kagari/styles/theme.css`、`src/themes/fuyukawa-kagari/pages/HomePage.astro`。

验证：`npm test` 13/13 通过；`npm run build` 12 页与性能预算全部通过；重启 dev 后确认页面内联 CSS 已无 `cursor-blink`、`profileCharDelay = () => 68` 已生效。

## 20. Blank 文案与右键菜单对齐现状（2026-09-13）

具体行为：

- Blank 首页 aside 文案：「这里不加载角色背景、Live2D、音乐、樱花或 Kagari 主题样式。」→「这里不加载主题壁纸、Live2D、音乐、雪花或 Fuyukawa Kagari 主题样式。」（角色图已换成照片壁纸、樱花雨已换成雪花、主题名写全）。
- 修复右键菜单的用户可见文案不一致：静态 HTML 写的是「❄ 雪花：开」，但 `updateContextToggleLabels()` 加载后会覆盖成「✿ 樱花雨：开/关」。现改为输出「❄ 雪花：开 / ❄ 雪花：关」；内部变量名、`data-context-action`、localStorage key 等标识符保持 `sakura*` 不变（遵循不改内部标识符的约定）。
- `README.md` 两处「樱花雨」描述改为「雪花」。

改动位置：`src/themes/blank/pages/HomePage.astro`、`src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`、`README.md`。

验证：`npm test` 13/13 通过；`npm run build` 12 页与性能预算全部通过；重启 dev 后确认 Blank 新文案生效、右键菜单标签为雪花，全仓 src 与 README 已无「樱花」字样。

## 21. Bangumi 追番接入（2026-09-13）

背景：Blank 的 Anime 一栏在 Fuyukawa 看不到，原因是 `animeFavorites` 是空数组——Fuyukawa 用 `length > 0` 隐藏整块，Blank 则无条件渲染标题，于是留下一个空标题。本次把 Anime 改成由 Bangumi 收藏数据驱动，并让 Blank 的空区块也自动隐藏。

具体行为：

- 新增 `scripts/sync-bangumi.mjs` + `npm run sync:bangumi <uid>`：优先用 v0 接口 `https://api.bgm.tv/v0/users/{uid}/collections?subject_type=2`（每页 50 条循环翻页），失败则回退旧版 `https://api.bgm.tv/user/{uid}/collection?cat=anime`；写入 `src/core/data/bangumi.json`，原始响应备份在 `tmp/bangumi-raw.json`，并下载封面（默认只下「在看」，`--all-covers` 可全下）转 WebP 存到 `public/bangumi/`。
- 新增 `src/core/data/bangumi.ts` 与初始空数据 `src/core/data/bangumi.json`：状态映射 1 想看 / 2 看过 / 3 在看 / 4 搁置 / 5 抛弃，导出全部条目、在看、其余、状态标签与显示名。
- Fuyukawa About 新增「追番记录」区块：在看的前 12 部用封面卡片展示，其余（含超过 12 部的在看）用文字行列出，附评分与 Bangumi 链接，末尾注明数据更新时间；数据为空时整块隐藏。
- Blank About 的 Anime 改为**表格**（名字 / 状态 / 评分，中文名为主、原名小字），展示全部条目、不含封面；Bangumi 数据为空时回退旧的 `animeFavorites` 列表，两者都空则整块隐藏。同时 Tech line、XP / Games 也改为空数据时隐藏，不再留空标题。

网络说明（重要）：本机所在网络对 Bangumi 域名存在 DNS 污染（`api.bgm.tv` 被解析到 `108.160.163.102` / `199.59.148.97`），且 Codex 沙箱对非白名单域名的 HTTPS 一律阻断，因此**同步必须在你的普通终端里执行**：

```powershell
$env:HTTPS_PROXY="http://127.0.0.1:7890"   # FlClash 的本地端口
npm run sync:bangumi -- <你的UID>
```

验证：临时样例数据下两个主题均正确渲染（Fuyukawa 封面卡 + 文字行，Blank 表格）；清空数据后样例不存在、空标题隐藏；`npm test` 14/14 通过；`npm run build` 12 页与性能预算全部通过；`node --check scripts/sync-bangumi.mjs` 语法通过、缺 UID 时输出用法提示。

## 22. Me 页改为下拉入口 + 新增 Anime 独立页（2026-09-13）

具体行为：

- Fuyukawa Me 页：移除内联的「追番记录」区块，改为「分类」下拉（原生 `details/summary`，默认收起，展开后是 Anime 入口 + 条目数），指向新页面 `/anime/`；下拉始终存在（未同步时显示「尚未同步」），避免入口在空数据期消失。
- 新增 Fuyukawa 独立页 `AnimePage.astro` → `/anime/`：顶部玻璃标题卡 + 「在看」封面卡片网格（全部在看条目）+ 看过/搁置/想看/抛弃分组文字清单 + 空状态提示。
- 新增 Blank 独立页 `AnimePage.astro` → `/themes/blank/anime/`：一张表格（名字 / 状态 / 评分，全量、无封面），沿用 `blank-table` 样式。
- Blank Me 页：Anime 区块改为 `blank-menu` 下拉，指向 `/themes/blank/anime/`；`animeFavorites` 若填写仍作为补充列表显示。
- 路由与导出：`src/pages/anime.astro`、`src/pages/themes/blank/anime.astro`；两个主题 `index.ts` 均导出 `AnimePage`。
- 同步脚本增强：封面优先取 `medium(800)` → `common(400)` → …（原来优先 `grid`，只有 100px 会发虚）；`--from-file` 支持 `rawEntries` 结构（可用 `tmp/bangumi-raw.json` 重跑而无需再打接口）；新增 `--dry-run` 只解析不写文件。

同步结果（用户本机执行）：uid `1028220`，共 **203 部**（在看 16 / 看过 95 / 想看 74 / 搁置 16 / 抛弃 2），在看封面已下载到 `public/bangumi/`。

待办：现有封面是旧的 100px 版本，用下面的命令重跑即可升级为 400px（只需要网络下载封面，接口数据来自本地 raw dump）：

```powershell
$env:HTTPS_PROXY="http://127.0.0.1:7890"
node --use-env-proxy "C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\scripts\sync-bangumi.mjs" --from-file="C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\tmp\bangumi-raw.json" --uid=1028220
```

验证：`npm test` 14/14 通过；`npm run build` 生成 14 页（新增 `/anime/` 与 `/themes/blank/anime/`），Pagefind 索引 14 页，性能预算全部通过；重启 dev 后确认 Me 下拉、`/anime/` 真实数据（16 部在看 + 本地封面）、Blank 表格 203 行均正确。

## 23. Bangumi 链接、Anime 入口改链接、两栏密排（2026-09-13）

具体行为：

- `profileIdentity.bangumi = "https://bgm.tv/user/1028220"` 加入社交链接单一来源；首页英雄区新增第三个（外链图标）按钮、Fuyukawa Me 的 Profile README 新增 Bangumi 按钮、Blank 的 About 操作行新增 Bangumi 链接，三处都由同一字段驱动，留空即隐藏。
- Me 页的 Anime 入口由 `details` 下拉改为**直接可点的文字链接**（`me-more-link` / Blank 的 `blank-link-row`：图标 + 下划线文字 + 条目数 + 箭头），并删除 Fuyukawa 侧「追番完整列表放在独立页面，这里只留入口」这句话。这样与不可点击的「最近的电波」方块卡片在观感上明确区分。
- 排序改为 在看 → 看过 → **想看 → 搁置** → 抛弃（`src/core/data/bangumi.ts` 导出时排序 + 同步脚本 `STATUS_ORDER` + `AnimePage.astro` 分组顺序三处一致）。
- Fuyukawa `/anime/` 的「看过/想看/搁置/抛弃」清单改为**两栏密排**（`grid-template-columns: repeat(2, minmax(0,1fr))`，行高 6px、字号 0.88rem，移动端回落单栏）；「在看」保持封面卡。
- Blank `/themes/blank/anime/` 表格由三列改为**两列**（名字 / 状态，评分并入状态列显示为「状态 · N 分」），行内边距 10px → 6px、字号 0.88rem，更紧凑。
- 封面清晰度：脚本已改为优先取 `medium(800)` 源、输出 480px WebP（原为 grid 100px）。**需要用户在本机重跑一次同步**才会生效，命令见下。
- Bangumi 图标调研：`simple-icons` / `tabler` / `arcticons` / `@iconify-json/cib` / `@iconify-json/logos` / `@iconify-json/icon-park` 均**没有** Bangumi 标志；npm 上 8 个开源 Bangumi 插件包（bilibili-bangumi-component、astro-bangumi、hexo-* 系列等）也只含截图不含 logo。能取到官方 logo 的域名（bgm.tv / GitHub / jsDelivr / Wikimedia）在本机与沙箱均不可达，故暂时使用通用 `tabler:external-link` 图标。

封面升级命令（只需联网下载 16 张封面，接口数据读本地 raw dump）：

```powershell
$env:HTTPS_PROXY="http://127.0.0.1:7890"
node --use-env-proxy "C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\scripts\sync-bangumi.mjs" --from-file="C:\Users\luosh\Documents\ChatGPT\Codx project\fuyukawa-blog\tmp\bangumi-raw.json" --uid=1028220
```

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 与 dev 均确认 `/anime/` 清单为两栏、Blank 表格为两列（203 行 + 表头），英雄区/Me/Blank 三处 Bangumi 链接均已输出。

## 24. Anime 列表改为「·」流水式（2026-09-13）

具体行为：

- Fuyukawa `/anime/`：看过 / 想看 / 搁置 / 抛弃四组由两栏行式网格改为**内联流水**（`p.anime-inline`，条目间以 `·` 分隔、自动换行、行高 2.05、字号 0.92rem）；去掉每个条目中文名之后的日文原名；评分也不再显示。正在看的封面卡片同步移除了日文原名小字。
- Blank `/themes/blank/anime/`：整体表格替换为按状态分组的 `p.blank-anime-inline` 流水列表（同样 `·` 分隔、无日文名、无表格结构），标题右侧保留「N 部」。
- 条目本身仍是可点链接：平时无下划线，hover / 键盘聚焦时变粉并在文字下方出现下划线（`border-bottom` + `currentColor`）。
- 效果示例（实际渲染）：`幸运星 ·寒蝉鸣泣之时 ·轻音少女 ·凉宫春日的忧郁 …`

改动位置：`src/themes/fuyukawa-kagari/pages/AnimePage.astro`、`src/themes/blank/pages/AnimePage.astro`、`src/themes/blank/styles/theme.css`。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 与 dev 均确认两个主题的 Anime 页已是「·」流水式、日文原名不再输出。

## 25. Anime 入口加框 + 悬停变暗、标题宽度对齐（2026-09-13）

具体行为：

- Fuyukawa Me 页的 Anime 入口恢复成**方框**（浅纸底 + 1px 描边 + 圆角 + 轻阴影，内边距 11/14px），hover / 键盘聚焦时整框**变暗**（`background: rgba(226,219,226,.85)`、描边加深），明确表示可跳转；同时保留文字下划线与箭头右移的反馈。
- Fuyukawa Me 页 Profile README 的 GitHub / Bilibili / Bangumi 胶囊按钮同样加悬停变暗（`transition: background 160ms ease, border-color 160ms ease`）。
- Blank Me 页：Anime 入口 `blank-link-row` 改为带描边的方框 + hover 变暗（`#e6e8ec`）；`blank-actions` 的链接按钮同样加 hover 变暗（首个深色按钮用 `#31353d` 保持文字可读）。
- Fuyukawa `/anime/` 的标题卡新增 `anime-hero` 类并 `max-width: none`，宽度与下方 `.anime-shell` 卡片一致（都是 `min(1160px, 100% - 32px)`，原来被全局 `.page-title` 的 `max-width: 780px` 压窄）。
- 所有悬停效果都包在 `@media (hover: hover) and (pointer: fine)` 内，避免触屏误触发。

改动位置：`src/themes/fuyukawa-kagari/pages/AboutPage.astro`、`src/themes/fuyukawa-kagari/pages/AnimePage.astro`、`src/themes/blank/styles/theme.css`。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 中确认 `.me-more-link[data-astro-cid]:hover`、`.about-link-row a:hover`、`.blank-link-row:hover`、`.blank-actions a:hover`、`.anime-hero{max-width:none}` 均已生成。

## 27. 下级页面返回按钮 + 页脚访问统计（2026-09-19）

具体行为：

- 新增共用组件 `src/core/navigation/BackButton.astro`（内联 `onclick`：站内历史优先 `history.back()`，否则走 `href` 兜底）与 `src/core/navigation/backTarget.ts`（层级兜底：文章 → `/blog/`；`/anime/` → `/about/`；其余 → 首页，自动带主题前缀）。
- 两个 layout 注入：Fuyukawa 在 `<main>` 里对 `surface !== "cover" && !isArticlePage` 的页面渲染返回按钮（首页与已有「回到文章列表」的文章页不重复加）；Blank 对非首页（`canonicalPath !== "/"`）渲染。样式分别写在两套 theme.css（Fuyukawa 玻璃胶囊、Blank 方框），悬停变暗。
- 页脚把「旅行者一号 / 二号」两行替换为访问统计：
  - `📈 本站总访问：N 次`
  - `📅 今天你是第 M 位来访者`
  （保留原来的 🌸「Mikan KenkyuuSho 已落地」计时行。）
- 计数实现：客户端请求免费免注册的计数服务 `abacus.jasoncameron.dev`，用两个 key —— `total` 与 `day-YYYY-MM-DD`（Asia/Shanghai 日期）；每次**会话只计一次**（sessionStorage 标记），页站内跳转不会重复累加；结果缓存进 localStorage，服务不可用时展示上次数值或占位符 `—`。本地预览使用独立命名空间 `mikan-blog-dev-probe`，真实站点用 `mikan-fuyukawa-blog`，互不污染。

待确认（重要）：该计数依赖第三方免费服务，Codex 沙箱与用户当前网络都无法验证其可达性。若页脚显示 `—`，说明该服务不可达，备选方案为：① 不蒜子（国内稳定，但只有总访问量/访客数，没有「今日第几位」）；② Cloudflare Worker + KV 自建（两个数字都有，需要一个免费 Cloudflare 账号）。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 确认页脚两行统计已替换、旅行者文案已移除，计数脚本已打包进 `BaseLayout.astro_astro_type_script_index_0_lang.*.js`，`/themes/blank/*` 等下级页均带返回按钮且 href 正确（`/anime/` → `/about/`，其余 → 首页/主题首页）。

## 28. 返回按钮收窄到「有父级的二级页面」（2026-09-19）

用户反馈：一级页面不该出现返回按钮。按 grilling 轮次确认后修改（1a 2a 3a 4a 5 一样 6 不放）：

- `src/core/navigation/backTarget.ts` 返回值改为 `string | null`：只有存在真实父级的页面才给目标（文章 → `/blog/`、`/anime/` → `/about/`），`/blog/`、`/projects/`、`/about/`、`/games/`、`/404`、首页一律返回 `null`。
- 两个 layout 的渲染条件简化为 `{backTarget && (…)}`，不再依赖 `surface` / `isArticlePage` / `isHome` 判断。
- 文章页原有的「← 回到文章列表」内联链接删除，改用统一的顶部返回按钮（位置与样式全站一致）；`.back-link` 样式保留，因为 404 页的「← 回到首页 / 打开 Blank 主题」仍在用。
- 返回行为不变：优先站内历史 `history.back()`，没有历史时按父级兜底。

实测（dist 与 dev 逐页核对）：只有 `/anime/`（→ `/about/`）与 `/themes/blank/anime/`（→ `/themes/blank/about/`）带返回按钮；首页、blog 列表、about、projects、games、404 全部没有。文章页因目前没有已发布文章，构建不出该路由，逻辑已就位，发第一篇后会自动出现。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过。

## 29. Me 页新增友链卡片（2026-09-19）

具体行为：

- 新增友链数据源 `src/core/data/friends.ts`（`FriendLink { name, url, avatar?, description? }`），Fuyukawa 的 Me 页与 Blank 的 About 页共用；留空则整个区块隐藏。
- 首条友链：**Elysia**（`https://seele.wiki`）。站点头像取自对方站点页头使用的 `/_astro/3.FaJNP5W6_1WDA1r.webp`（原图 6000×8416，站点自身以 `object-position: center` 展示），按同样的居中方式裁成 256×256 WebP（16.8KB），存放于 `public/friends/seele-wiki.webp`（主题中立的公共目录，两个主题都能引用）。
- Fuyukawa Me 页在「最近的电波」下方新增「友链」卡片：`auto-fill minmax(200px,1fr)` 网格，每张卡是「圆形头像 + 名字（+可选一句话）」，悬停整卡变暗并轻微上浮；没有头像时用名字首字做圆形占位（不自行生成图片）。
- Blank About 页新增 Friends 区块：`blank-friend-row` 行卡（36px 圆头像 + 名字），悬停变暗，与其它的 Blank 链接风格一致。
- 未加「申请友链」提示行（用户没要求），需要的话一句话就能补。

改动位置：`src/core/data/friends.ts`、`src/themes/fuyukawa-kagari/pages/AboutPage.astro`、`src/themes/blank/pages/AboutPage.astro`、`src/themes/blank/styles/theme.css`、`public/friends/seele-wiki.webp`。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 与 dev 均确认 `/about/` 输出 `friend-card` + `/friends/seele-wiki.webp` + `Elysia` + `https://seele.wiki`，Blank 侧输出 `blank-friend-row`，头像文件已随构建复制到 `dist/friends/`。

## 30. 404 保留 + blog 搜索框占位符清理（2026-09-20）

- 404 页面：用户确认**保留**（理由：GitHub Pages 用根目录 `404.html` 兜底，且该页承载 `/themes/blank/*` 的重定向脚本），本次未做任何改动。
- blog 搜索框占位符：`placeholder="输入 AstrBot、插件、部署..."` → `placeholder="输入关键词…"`，清掉旧站遗留的示例关键词（`AstrBot` 在全项目仅此一处；Blank 的 blog 页没有搜索框，无需处理）。搜索框上方的「关键词」标签保持不变。
- 测试中新增断言：`BlogIndexPage.astro` 不得再出现 `AstrBot`，且占位符为 `输入关键词…`。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dev 预览确认 blog 搜索框已输出新占位符、页面内不再含 `AstrBot`。

## 31. 深色模式 + 右下角快捷操作（2026-09-20）

用户要求"做一个黑暗模式，右下角切换 Blank 的地方拉出两个圆形图标（暗色 + 切主题）"，并按 grilling 轮次确认：1a 全站共用一份偏好、2a 精选覆盖、3a 默认跟随系统、4a 深蓝灰、5a 保留首页照片只压夜色、6a 桌面 hover 展开 / 触屏也能用。

具体行为：

- 新增 `src/core/ColorMode.astro`（`<head>` 内联脚本，渲染前生效，避免闪白）：偏好键 `mikan-color-mode-v1`，取值 `auto | light | dark`，默认 `auto`（跟随 `prefers-color-scheme`）；设置 `html[data-color-mode="light|dark"]`、`data-color-mode-preference`、`color-scheme`；暴露 `window.__mikanColorMode`（`resolved()/set()/toggle()`）；用 document 级事件委托绑定 `[data-color-mode-toggle]`，所以客户端路由切换后依旧可用；系统主题变化时若为 `auto` 会实时跟随。
- 右下角 UI：原来的单个 `theme-quick-switch` 换成 `quick-actions` 圆形按钮组 —— 默认只露调色板（切 Blank），桌面 hover / 键盘 focus 时月亮按钮从左侧滑出；触屏（`hover: none`）两个按钮常显，保证手机能切。暗色开启时按钮变月亮↔太阳图标并同步 `aria-pressed`。Blank 侧也放了同一组（调色板切回 Fuyukawa）。
- 深色配色（v1 精选覆盖，深蓝灰）：重定义 `--ink/--paper/--paper-soft/--glass-*` 等 token，再覆盖结构性表面 —— 页面底色与网格、首页封面与下半背景（保留照片 + 压暗遮罩）、页头导航胶囊、页脚（含文字色与阴影）、以及各类卡片/面板/表单（`:is(...)` 分组一次性覆盖 page-title、post-card/row、project-card、article、about-*、works-*、anime-block、friend-card、toy-widget、通知弹窗、音乐面板等），并让卡片内文本继承浅色、保留粉蓝小标题点缀；prose 正文、目录、搜索框、右键菜单、通知遮罩、滚动提示、分隔条都做了对应处理。Blank 主题变量少，直接翻转其调色板 + 覆盖页头/页脚/边框色。

已知范围（v1 取舍）：精选覆盖不是全量变量化重构，个别装饰细节（贴纸/胶带、部分半透明渐变、音乐控件内部等）在暗色下可能仍偏亮；用户目视发现后按处修补即可。

改动位置：`src/core/ColorMode.astro`（新增）、`astro.config.mjs`（新增 moon/sun 图标）、`src/themes/fuyukawa-kagari/layouts/BaseLayout.astro`、`src/themes/blank/layouts/BlankLayout.astro`、`src/themes/fuyukawa-kagari/styles/theme.css`、`src/themes/blank/styles/theme.css`。

验证：`npm test` 15/15 通过；`npm run build` 14 页与性能预算全部通过（Home CSS 120.0 KiB / 312.5 KiB）；dist 与 dev 均确认 `/`、`/blog/`、`/themes/blank/` 等页面都输出偏好脚本、`data-color-mode-toggle` 与 `data-quick-actions`，CSS 内含 `html[data-color-mode=dark]` 覆盖与 hover 展开规则。

## 32. 深色模式四个问题的修复（2026-09-20）

用户实测反馈四点，逐条修：

1. **换页后回到亮色、且要点两次才回暗色**：根因是 `ColorMode.astro` 的守卫 `if (window.__mikanColorMode) return;` 写在最前面——脚本在换页后重跑时直接退出，不再写回属性；而 `ClientRouter` 换页会重置 `<html>` 上的 `data-*`，于是回到亮色。此时内部 `resolve()` 仍认为当前是暗色，第一次点击便把偏好设成 light（视觉无变化），第二次才切回 dark，表现为"要按两次"。修复：脚本加 `data-astro-rerun`，把"重新读取偏好并写回属性"的 `apply()` 提到守卫之前，并额外监听 `astro:after-swap` / `astro:page-load` 再补一次（监听器仍只注册一次）。
2. **可点击模块悬停仍是亮色、字看不清**：为该主题补了暗色交互态覆盖 —— back-button、about-link-row、music/live2d/右键菜单按钮、导航链接、通知关闭、社交链接、游戏按钮、文章目录、works 筛选与展开、搜索结果、me-more/friend 卡片的 hover/focus/active 统一改成半透明白底 + 亮字，`aria-pressed` 的主题项同处理；`.works-filter button` / `.works-card-toggle` 补基础底色；代码块复制按钮的 hover 也一并对齐（覆盖层使用 `!important` 以压过 ProjectsPage 等组件作用域样式）。
3. **Blank 深色下白字消失**：`.blank-skip-link` 与 `.blank-actions a:first-child` 是 `color:#fff` + `background: var(--blank-ink)`，暗色下 ink 变浅 → 白字压在浅底上。现在暗色下反色为浅底深字（hover 用纯白底），Blank 的 `#e6e8ec` 浅灰 hover 也换成深色可读样式。
4. **触屏"点两下展开/收起"未实现**：用户要求简化为"悬停拉出两个圆钮"。现在桌面：默认只露调色板，hover/focus 向左拉出月亮（暗色）与调色板（换主题）；触屏（`hover: none`）两个常显，不再依赖双击；容器左侧加 12px 热区便于扫入。

验证：`npm test` 15/15 通过；`npm run build` 14 页与性能预算全部通过（Home CSS 121.0 KiB）；dist 确认内联脚本带 `data-astro-rerun` 与 `astro:after-swap` 监听、暗色交互态覆盖（`color:#f2f6ff!important`）、Blank 反色规则（`#12151c` / `#e8ecf4`）均已生成。

## 33. 右下角改成"把手 + 拉出两钮"模块；电波卡换玻璃底；音乐标签提亮（2026-09-20）

按 grilling 轮次确认（1a 替换旧的那组、2a 收起只露把手、3a 桌面 hover / 触屏点击、4a 只改三张内容卡、5a 两种模式统一）：

- 新增共用组件 `src/core/QuickActions.astro`（两个 layout 共用，替换原先各自内联的按钮组）：
  - 收起状态只露一个圆形**把手**（⋯ dots-vertical）；桌面 hover / 键盘聚焦 / 钉住时，左侧**拉出两个圆钮**（月亮＝暗色、调色板＝换主题）。
  - 点把手 = 钉住 / 取消钉住（`is-pinned`），`aria-expanded` 同步，把手图标展开时旋转 90°；点菜单里的按钮不会收起；点页面其它地方或按 Esc 收起。
  - 触屏（`hover: none`）默认收起，**点把手展开、再点把手（或点空白）收起**，实现了用户要的"点击两下收回"。
  - 暗色偏好与主题切换仍分别复用 `[data-color-mode-toggle]` 与 `[data-theme-select]` 的既有委托逻辑，所以切换行为不变。
- 「最近的电波」三张卡片换成**与首页时钟块 `.home-signal-card` 完全同款**的玻璃底（`linear-gradient(150deg, .14/.06/.04)` + `rgba(255,255,255,.08)`、1px 白边、16px 圆角、2px 模糊），暗色下对应深色玻璃；文字走 `var(--ink)/var(--ink-soft)`，暗色下不再看不清。
- 音乐播放器说明文字在深色下提亮：`.music-control-label`（播放进度 / 音量）、`.music-cover-caption`、`.music-meta`、`.music-note`、`.live2d-status` 统一为 `#c3cee2`，时间数值用 `var(--ink)`。

验证：`npm test` 15/15 通过；`npm run build` 14 页与性能预算全部通过（Home CSS 122.1 KiB / 312.5 KiB）；dist 与 dev 均确认三个页面输出 `data-quick-actions-menu` + `data-quick-actions-handle`，CSS 内含 `.quick-actions:hover .quick-actions-menu`、`.quick-actions.is-pinned .quick-actions-menu`、电波玻璃底与音乐标签提亮规则。

## 34. 预览页：文章阅读进度栏 + 项目卡片拼贴（2026-09-20）

按 grilling 轮次确认（1a 左侧固定栏、2a 列 H2+H3、**3b 窄屏收成顶部细条**、4a 数据声明尺寸档、**5 悬停文字从左侧淡入**、6a 原地覆盖层 + 共享元素放大、7a 详情含技术栈与链接、8b 两个独立预览页、**9a 占位示例数据**）。**本次只新增预览页，正式页面一行未改。**

- `/preview/reader/`：示例文章 + 左侧阅读栏。宽屏（>1100px）左侧常驻「竖向进度轨道 + H2/H3 标题列表」：读过的标题变淡、当前标题加粗并带粉色圆点，点标题跳锚点；滚动时用一次 rAF 节流监听同时算阅读百分比与当前标题。窄屏隐藏整栏，改为顶部 3px 进度条。
- `/preview/projects/`：6 个占位项目组成的错落拼贴（尺寸档 small / wide / tall / big 由数据声明，网格 `auto-fill minmax(220px,1fr)` + `grid-auto-rows: 92px`）。卡片图片铺满，悬停时图片压暗（brightness .48）并轻微放大，说明文字**从左侧淡入**（`translateX(-18px) → 0`）；点击整张卡用 View Transitions 的共享元素把卡片"放大"成居中详情面板（大图 + 状态/年份 + 简介 + 详述 + 技术栈标签 + 外链），Esc / 点背景 / × 关闭；`prefers-reduced-motion` 时退化为直接显示隐藏。
- 两个页面都加 `noindex,nofollow`（`BaseLayout` 新增 `noindex` 属性），并从 sitemap 过滤中排除 `/preview/`；合并进正式页面后会整体删除。
- 踩到一个 Astro 编译器的坑：`<template>` 内渲染 `<a>` 会产出非法 JS（构建报 `Expected ")" but found "{"`）。改为把项目数据用 `<script type="application/json">` 内联、详情 DOM 由 JS 构建，绕开该问题。
- 预览用的图片全部复用站内已有资源（hero 壁纸、about/ 下的番剧图、profile 等），并明确标注"示例数据，仅用于查看效果"。

验证：`npm test` 16/16 通过；`npm run build` 16 页（新增 `/preview/reader/`、`/preview/projects/`）与性能预算全部通过；sitemap 中不含 `preview`；dist 与 dev 确认两页 200、带 noindex、阅读栏与 6 张卡片均正确输出。

## 35. 暗色切换不再位移 + 阅读栏改横条 + 快捷模块收敛 + 卡片回弹（2026-09-20）

用户实测反馈四条：

1. **首页切暗色时图片偏移、且是瞬切**：根因是暗色覆盖里把「渐变遮罩 + 图片」写在同一条 `background` 声明里重写，切换瞬间整条声明被替换。现改为：**图片那条声明在两种模式下完全一致**，变暗只靠独立遮罩层（`.hero-overlay` 深色遮罩、`.home-content-bg::before` 深色渐变），`html[data-color-mode="dark"] .hero-stage/\.hero` 的带图重写已删除；同时明暗切换改为 **480ms 交叉淡入**（`document.startViewTransition` + `html.is-color-mode-switching::view-transition-*` 覆盖时长，`prefers-reduced-motion` 时直接切换）。
2. **阅读栏改为常驻左侧、收起只显示长短横条**：`.reader-rail` 从 sticky 改成 `position: fixed; left: 16px; top: 50%`；每个标题是一条 3px 横条（H2 26px、H3 16px、当前 34px），当前为黑色（`#1b2028`）、已读淡灰、未读更淡；悬停 / 聚焦才向右浮出标题文字面板可点跳，移开只剩横条提示相对位置；暗色下自动反色（近白 + 淡灰）。窄屏仍隐藏、保留顶部 3px 进度条。
3. **右下角点两下再移开不收回**：原因是"点菜单里的按钮保持展开"这段逻辑，钉住状态不会被清掉。现改为桌面**彻底取消钉住**（纯 hover，移开即收），触屏点把手展开 / 再点收起，且**触屏点过菜单按钮后自动收起**。
4. **卡片放大不够曲线、详情文字贴边**：共享元素过渡加回弹曲线 `cubic-bezier(0.34, 1.56, 0.64, 1)`（420ms，约 4% 过冲），通过 `html.is-project-open::view-transition-group(*)` 生效；详情内容内边距 26/28px → **32/38px**（移动端 22/20px），标题、简介、标签、链接间距一并放宽。

验证：`npm test` 16/16 通过；`npm run build` 16 页与性能预算全部通过（Home CSS 121.9 KiB）；dist 确认回弹曲线、新内边距、`is-color-mode-switching` 规则均已生成，且暗色块中不再存在带图片的 `.hero-stage/.hero` 重写；dev 确认阅读页输出 8 条横条 + 8 个标题标签、栏位为 `position: fixed`。

## 36. 阅读条按新规格重做 + 快捷模块再点收起 + 正文恢复原宽 + 卡片详情收尾（2026-09-21）

用户实测反馈四条，逐条处理（本条只动预览页与共用组件，**正式文章布局一行未改**）：

1. **阅读进度条推倒重做**：原来那套「一列长短横条 + 悬停浮出标题」全部删掉，改成一条固定长度的细轨道。
   - **位置**：`position: fixed`，垂直方向在 `top: 40vh`（中上）、整条长度 `clamp(220px, 36vh, 340px)` 不随滚动变化、宽 4px 圆角。横向位置由脚本量出 `--reader-rail-left`：取 `clamp(正文左缘 - 20px, 音乐抽屉宽度 + 12px, 正文左缘 - 10px)`——即优先贴在正文左边，但**不会钻进左下角音乐抽屉展开后的范围**，也不会压到正文（`正文左缘 = article.left + paddingLeft`）。
   - **颜色**：轨道底色浅灰 `rgba(27,32,40,.14)`、当前区段近黑 `#1b2028`；深色模式整条反色（轨道 `rgba(242,245,251,.18)`、区段 `#eef2f9`）。
   - **当前区段**：正文里有多个 H1/H2/H3 时点亮「当前章节」那一段（当前标题 → 下一个标题的比例）；没有章节的长文则点亮「顶部 → 当前滚动比例」。用 `transform: translateY(x%) scaleY(len)`（`transform-origin: top center`）驱动，滚动时**不加 transition**，避免进度条滞后于滚动。
   - **悬停**：鼠标在轨道上移动时，光标处长出一条 6×46px 的条带标记（`transform: translateX(-50%) scaleY(0) → 1`，`transform-origin: center`，180ms `--ease-out`；`translateX(-50%)` 是为了让 6px 的标记正中压在 4px 的轨道上），同时**往左**浮出离该位置最近的标题；移开收缩消失。标题往左而不是往右，是因为轨道已经贴着正文左缘，往右会盖住字。
   - **标题数据**：加载时自动收集正文里的 H1/H2/H3 及其在文中的位置（`article.querySelectorAll("h1, h2, h3")`），滚动时不再重复读 DOM；悬停位置最接近哪个标题就显示哪个。
   - 另外：轨道只有 4px，命中区用 `::before`（`inset: -12px -14px`）放宽；点轨道跳到最近标题（`prefers-reduced-motion` 时直接跳）；给屏幕阅读器留了一份视觉隐藏的标题跳转列表；`prefers-reduced-motion` 下去掉伸长与位移，只保留淡入淡出。
2. **右下角快捷模块「点两下收起」**：根因是桌面端把手点击被 `touchOnly` 直接 `return` 掉，而 hover / `:focus-within` 又一直把菜单撑着，所以怎么点都收不起来。现在把手在**桌面和触屏是同一套开关**：点一下 `is-pinned` 展开并保持，再点一下收起；收起时加 `is-collapsed` 抑制态（规则写在 hover / focus-within 之后，指针还停在模块上也能收），指针离开模块时清掉。鼠标移开模块仍然即收（`pointerout` + `pointerType === "mouse"`；触屏不触发，否则点开会立刻被关掉）。`aria-expanded` 继续跟着 `is-pinned` 走。
3. **电脑上正文变窄靠左**：根因就在预览页自己——`.reader-shell` 用了 `250px + 1fr` 的网格给进度栏留一列，正文又被 `max-width: 760px` 二次收窄，于是既比正式文章页窄（860 → 760），又在右边留出一大片空白。现在外壳网格整个删掉，`.reader-article` 直接 `width: min(860px, calc(100% - 32px)); margin: 104px auto 72px`，与正式文章页同宽同位置；进度条改成不占位的浮层。
4. **项目卡片详情收尾**：
   - 去掉详情面板左上角那个 × 关闭按钮，改成「点卡片以外的背景」或 Esc 退出。
   - 放大／收起的曲线从 `cubic-bezier(0.34, 1.56, 0.64, 1)`（约 4% 过冲）+ 420ms 换成主题既有的 **`--ease-pop`**（`cubic-bezier(0.18, 1.08, 0.25, 1)`，约 1.5% 的轻微回弹）+ 380ms。
   - **收起时原先根本没有回弹**：回调里把 `is-project-open` 摘掉，时长/曲线规则当场失效。现在收起前先挂 `html.is-project-closing`，等 `transition.finished` 再摘，进出用同一套曲线。
   - 详情大图贴住面板上沿、只做圆角矩形（`margin: 0` + `border-radius: 17px 17px 14px 14px`，对齐面板 18px 圆角）。
   - 详情文字单独限宽 `max-width: 34rem`，内边距 32/38px → **34/48px**（移动端 22px），四周不再贴边。

验证：`npm test` 16/16 通过；`npm run build` 16 页与性能预算全部通过（Home CSS 122.0 KiB）；dist 确认阅读页输出 `--reader-rail-left` / `scaleY(0)` / `transform-origin:center` / `position:fixed` / `min(860px,…)`，项目页已无 `.project-close`、含 `is-project-closing`、`var(--ease-pop)`、`border-radius:17px 17px 14px 14px`、`max-width:34rem`，两个主题的 CSS 都含 `.quick-actions.is-collapsed .quick-actions-menu`；dev（4322）直接返回了新版页面，无需重启。观感（条带伸缩手感、回弹幅度、深浅色）仍需用户目视验收。

## 37. 右下角快捷模块改成胶囊（2026-09-21）

用户要求："右下角主题切换做成胶囊样式，交互不变"。

- 原来是「把手圆钮 + 向左拉出的两个圆钮」三个独立圆形。现在整块（把手 + 月亮 + 调色板）**共用一层胶囊底**：`.quick-actions::before` 画底（圆角 999px、白边、投影、`backdrop-filter`），三个按钮改成胶囊内的透明图标槽（`background: transparent; border: 0; border-radius: 999px`），悬停时被指的那个槽自己上一层浅灰底（暗色下是 `rgba(255,255,255,.14)`），图标的旋转/缩放从按钮本体移到 `svg` 上。
- **展开方式一行没改**：仍是默认只露把手、hover / `:focus-within` / `is-pinned` 向左展开；点把手开关、鼠标移开即收、再点一次用 `is-collapsed` 抑制。
- 胶囊伸缩不用宽高动画，而是**用 `clip-path`**：收起时 `inset(0 0 0 calc(100% - 52px) round 999px)` 只罩住右边 52px（正好是把手 + 两侧内边距，看起来是一颗小圆头），展开时放开成 `inset(0 0 0 0 round 999px)`，220ms `--ease-out`。两态之间是裁剪在动、不触发布局，图标本身仍走原来的 opacity + translateX 滑入。
- 暗色下胶囊底换成 `rgba(30,36,50,.9)` + 深色投影（这套原先画在每个圆钮上）。

验证：`npm test` 16/16 通过（新增 `::before` 胶囊底、两态 clip-path、暗色胶囊底的静态断言）；`npm run build` 16 页与性能预算全部通过（Home CSS 122.8 KiB）；dist 的 `BaseLayout.*.css` 确认收起/展开/抑制三态裁剪规则都已生成；dev 重启后 4322 已在提供新版样式（theme.css 改动不重启不生效，就是交接文档第 3 条那个坑）。**Blank 主题的快捷模块仍是原来的圆钮**，需要用户确认后再配那边的底色与边框。

## 38. 阅读条改成小横条、快捷模块点击目标修好、卡片收尾（2026-09-21）

用户实测反馈四条：

1. **右下角「第二次点击收不起来」的真正根因**：上一轮改成胶囊后整块看起来就是一个按钮，用户点的多半是**胶囊本体**而不是那 42px 的把手图标；旧逻辑里「点在模块内、但不在把手上」在桌面端直接 `return`，所以第二下什么都不做。现在分两类：点月亮 / 调色板走它们自己的功能；**点模块的其他任何位置（把手、胶囊本体、胶囊里的空隙）都算开关**——点一下展开并保持，再点一下收起。胶囊底另加 `cursor: pointer`（只对可见部分生效，`clip-path` 裁掉的部分不参与命中）。
2. **阅读条改成「一列小横条」**（上一轮我做成了单根竖条，理解错了）：一个标题一条，H1 34px / H2 26px / H3 16px，厚 3px、圆角、间距 7px，整串长度固定；底色浅灰，**当前章节那一条近黑**，其余保持浅灰（不再有「已读」中间态）。没有章节的长文用固定 12 根细条，从顶部按比例点亮。位置改成**文章栏左侧外面**（`rect.left - 26`，最小 72px），不再被左下角音乐抽屉的展开宽度往右推。悬停：光标那一行左侧长出一条 5×40px 标记（`scaleY(0) → 1`、`transform-origin: center`、180ms），同时在左边浮出最近的标题；点横条跳到最近的标题；`prefers-reduced-motion` 下去掉伸长与位移。
3. **卡片展开时图片位移**：原因是详情大图固定 16/9，而卡片有 small / wide / tall / big 四种比例，展开动画结束时图片会重新裁切。现在大图比例**跟着被点的卡片走**（JS 量卡片宽高写入 `--project-hero-ratio`，限幅 1.3–2.0），构图不再跳。另外：**面板内部不再有滚动条**（`scrollbar-width: none` + `::-webkit-scrollbar`，滚轮照样能滚）；详情文字内边距 34/48 → **36px / clamp(30px, 6vw, 60px) / 46px**（移动端 28px），四周更松。
4. **卡片标题常驻 + 常驻暗色渐变**：`.mosaic-scrim` 从「悬停才出现」改成**常驻的由下到上暗色渐变**（`rgba(8,10,16,0) 34% → .32 62% → .78`），标题在任何图上都读得清；悬停时图片照旧压暗（`brightness(.48)`），而**说明文字从底下淡入**（`translateY(10px) → 0`，200/220ms），不再是原来的从左侧滑入。

验证：`npm test` 16/16 通过（预览页断言改成 `data-reader-bars`、`reader-bar--h2/h3`、`rect.left - 26`、`translateY(10px)`、`--project-hero-ratio`、`scrollbar-width: none`）；`npm run build` 16 页与性能预算全部通过（Home CSS 122.8 KiB）；dist 确认阅读页输出小横条与 `.left-26` 定位、项目页 CSS 含 `aspect-ratio:var(--project-hero-ratio, 16 / 9)` 与 `translateY(10px)`、`scrollbar-width:none`，首页内联脚本含新的事件分支与 `pointerout`；dev 重启后 4322 已在提供新版。**「小横条」是按「一个标题一条」理解的**，若用户指的是「整条只有一根横向进度条」，需要再改一次。

## 39. 修掉 scoped 样式失效（阅读条看不见 / 卡片详情没样式）+ Blank 胶囊 + 点两下收起（2026-09-22）

用户反馈五条，其中两条同一个根因：

1. **根因：Astro 的 scoped 样式匹配不上 JS 生成的节点**。Astro 会给模板里的元素加 `data-astro-cid-*`，而 `document.createElement` 造出来的节点没有这个属性。之前为「阅读条的小横条」和「卡片详情里的图片 / 文字 / 标签 / 链接」写的选择器全都匹配不上：**阅读条整串不可见**，**详情面板等于完全没样式**（文字贴边、图片不按比例、内部冒出滚动条）。现在这些选择器统一改成 `:global(...)`；`> *` 也要一并写成 `:global(.project-body > *)`，否则通配的那一层仍会被加上 scoped 属性（`aspect-ratio`、`max-width: 34rem` 这些都会失效）。
2. **卡片详情**：打开时同时锁 `html` 与 `body` 的滚动（原来只锁 body，滚轮还是会带走后面那一页），滚轮只在卡片内生效；内部滚动条保持隐藏，滚轮 / 触控板仍可滚；正文内边距改为 38 / clamp(32px, 6vw, 64px) / 48px（移动端 30px）。
3. **Blank 主题的右下角改成同一套胶囊**：底色走 `--blank-panel` / `--blank-line`（自带深浅色），收起裁剪 `calc(100% - 50px)` 对应 40px 的图标槽，展开 / 抑制 / 触屏三种状态与 Fuyukawa 一致。
4. **「点两下收不起来」的真正机制**：胶囊收缩时 `clip-path` 把底色从光标底下抽走，浏览器会补发一次 `pointerout`；旧写法只看 `relatedTarget`，于是被当成「指针离开模块」→ 清掉 `is-collapsed` → `:hover` 立刻又把胶囊撑开，看起来就是收不起来。现在 `pointerout` 处理器改用**坐标**判断指针是否真的还在模块矩形内。
5. 卡片放大的曲线没动，但整页交叉淡入从 380ms 缩到 220ms `--ease-out`，少几种动作叠在一起（用户说「震动多于 3 次」）。

验证（这次是真的用无头 Chrome 量的，不再是静态断言）：

- 新增 `tmp/verify-ui.mjs`：无头 Chrome + CDP，测量阅读条 / 卡片 / 胶囊 / Blank，并落截图到 `tmp/*.png`；
- 新增 `tmp/verify-pill.mjs`：用 `Input.dispatchMouseEvent` 派发**真实鼠标事件**复现「点两下」路径，并在页内装 `pointerout` 探针。
- 数据：阅读条 railLeft 335 / articleLeft 361（栏外 26px）、9 条横条全部可见（3px 高、非当前 `rgba(27,32,40,.2)`、当前 `rgb(27,32,40)`）、同时只有 1 条 is-current；卡片标题 opacity 1（常驻）、说明 opacity 0 + `translateY(10px)`、scrim 常驻渐变；详情 `padding 38/64`、大图 `aspect-ratio` 跟着卡片、`heroTop 1px`、圆角 17px、`scrollbar-width: none`、`html/body overflow hidden`、面板内部可滚；胶囊 hover → 展开，**click1 → pinned=true，click2 → pinned=false + is-collapsed=true，且 `:hover` 仍为 true 时裁剪确实收回、菜单 opacity 0**，全程 `pointerout` 记录 0 次；Blank 同样走完 expand→collapse。
- 坑：Chrome 的 `--user-data-dir` 必须放系统临时目录；放进项目 `tmp/` 会让 dev 的文件监听 EBUSY 崩掉（已踩过一次）。

## 40. 阅读条微调 + 两个预览页合并进正式页 + 胶囊抑制态修正（2026-09-22）

用户要求：阅读条再往左、条长剪短 1/3、取消悬停竖条、悬停文字去掉背景框；文章 h2 竖条与标题之间加一个空格；「可以合并这两个页面的功能进博客了」；右下角「点两下收起后移开又展开」。

1. **阅读条微调**：位置从「文章栏左缘 - 26」改成 **- 48**（更靠左）；条长各减 1/3：H1 34→**23px**、H2 26→**17px**、H3 16→**11px**（细条档 24→16px）；**删掉悬停的竖条标记**（`reader-hover-band` 整个移除，`.reader-bars::before` 命中区保留）；悬停标题**去掉背景框**，只留文字本身加一点与底色同向的文字阴影。
2. **抽成共用组件 `src/core/ReadingRail.astro`**（标记 + 脚本），样式全部放进 `themeSource`（`theme.css`）——放在主题样式里是全局的，也就不会再踩 scoped 那个坑。正式文章页 `ArticleLayout.astro` 与预览页 `/preview/reader/` 现在引用同一个组件。
3. **文章页合并**：撤掉右侧静态 `ON THIS PAGE` 目录（`article-toc`），`.article-shell` 从「860 + 240 两列」改成**单栏居中 860**，正文位置与预览页完全一致；`.prose h2` 的 `padding-left` 18→**24px**（竖条与标题之间多一个空格）。
4. **Projects 页合并**：拼贴卡片 + 详情面板搬进 `ProjectsPage.astro`（Fuyukawa），数据来自 `projects.ts`（新结构：summary/detail/status/year/tags/image/size/links，含 6 条从预览页搬过来的**示例数据**，换真货只改这一个文件）；对应样式（`.mosaic*`、`.project-*`、`html.is-project-open` 的滚动锁与 view-transition 时长）全部进 `theme.css`。Blank 的 Projects 页跟着新字段改用 `project.tags`。
5. **胶囊抑制态修正**：`pointerout` 时**不再**撤掉 `is-collapsed`，改成记 `dataset.quickActionsLeft`，只有指针真的离开过、再 `pointerover` 进来时才撤。这样「点两下收起」之后，指针在模块内外怎么动都不会被 `:hover` 重新撑开；重新悬停时照常展开。

验证：`npm test` **17/17**（新增「Article layout and projects page consume the shared rail and mosaic」）；`npm run build` 16 页 + 性能预算全过（CSS 119.9 KiB）。无头 Chrome 实测：阅读条 `widths = 23/17/11px`、`gapToArticle = 48`、`bandExists = false`、标题 `background rgba(0,0,0,0)` / `border 0px`；`/projects/` 卡片标题常驻、详情 `padding 38/64`、`heroAspect 2/1`（跟卡片）、`scrollbarWidth none`、`html overflow hidden`、点背景可关；Blank 胶囊 hover→开、click1→pinned、click2→收起、**再在模块内挪动 / 移开都不再展开**、重新进入才展开。

> 两个 `/preview/` 页面这一轮**先保留**：站里还没有已发布文章，预览页是阅读条的试验场；等你确认正式页观感后一起删（sitemap 过滤与 `noindex` 也一起收掉）。

## 41. 首页下半区文案与掉落标签换新（2026-09-24）

用户给了新文案与新标签清单（每条前面的 `#` 只是他的分隔符，标签本体不含 `#`）：

- 「哇，你终于回来了～」那句说明改成：**这里会落下一些我路过的时代，它们总是早盛早衰**。
- `HomePage.astro` 的 `fallingTags` 从原来那 14 个兴趣词换成 16 个：无路赛 / 你指尖跃动的电光 / 此生无悔 / 生女当如 / 螺旋升天，法力无边 / 快按住老虛的笔 / 如果奇迹有颜色 / 囍 / Awsl / 虎纹鲨鱼 / 老夫也不是什么恶魔 / 爷青回 / 嗨多磨 / 花Q / 谁让她拿到枪的 / DD斩首。
- 画布胶囊直接照 `label` 绘制，不额外拼 `#`，所以清单里没有加 `#`；一个标签一条胶囊，宽度按文字自适应。

验证：`npm test` 17/17；`npm run build` 16 页 + 预算全过；dist 与 dev 的首页 HTML 都能查到 16 个新标签（`data-tags` JSON）与新文案，旧文案已不存在。

## 42. 快捷胶囊：点开就固定，移开保持横式（2026-09-24）

用户要求（这一条推翻了第 35 条里「桌面纯 hover、移开即收」的结论）：**鼠标悬停点击后要固定住，挪开也保持横式；再点一下才收起。**

- 改法很小：`pointerout` 里**不再**取消 `is-pinned`（`if (event.pointerType === "mouse") group.classList.remove("is-pinned")` 整行删掉），只记 `dataset.quickActionsLeft` 标记，等指针重新 `pointerover` 进来时才撤掉 `is-collapsed` 抑制态。
- 结果：纯悬停仍然是临时展开（移开就收）；点一下 → `is-pinned`，移开也保持展开；再点一下 → `is-collapsed` 收起，并且移开不会自己弹开；重新悬停回来才会再次展开。点模块外 / Esc 仍然收起。

验证：真实鼠标事件实测 —— hover 菜单 1；click1 `pinned=true` 菜单 1；**移开 `pinned` 仍为 true、菜单仍为 1**（这正是用户报的 bug）；click2 `collapsed=true` 菜单 0；移开保持收起；重新进入才展开。`npm test` 17/17（新增两条断言：有 `quickActionsLeft`、不再有 `pointerType === "mouse"` 取消钉住）；`npm run build` 16 页 + 预算全过。

## 43. 快捷胶囊：鼠标点完残留的 :focus 会把它一直撑着（2026-09-24）

用户报：**收起后重新悬停回来，就变成常驻展开、移开也不收了。**

- 根因不在状态机，在 CSS：展开规则里写了 `.quick-actions:focus-within`。点击把手后按钮**保留焦点**（鼠标点击也是 `:focus`，只是不是 `:focus-visible`），于是「收起 → 移开 → 抑制态被撤 → 重新悬停」之后，`:focus-within` 依旧匹配，胶囊就被永久撑开了。第 35 条里「点菜单按钮后一直展开」的老毛病也是同一个原因。
- 改法：两个主题里把展开条件从 `:focus-within` 换成 **`:has(:focus-visible)`**（只有键盘 Tab 过来的焦点才算），并在收起时清掉鼠标点击残留的焦点（`if (focused && !focused.matches(":focus-visible")) focused.blur()`，键盘焦点保留，不影响 Tab）。

验证（真实鼠标事件）：hover 菜单 1 → click1 `pinned=true` → 移开仍 `pinned=true`+菜单 1 → click2 收起 → 移开保持收起 → **重新悬停菜单 1 → 再移开菜单 0（这次真的收回去了）**。`npm test` 17/17（新增断言：两主题都要有 `:has(:focus-visible)`，且不再有 `.quick-actions:focus-within`）；`npm run build` 16 页 + 预算全过。

## 44. 首页问候语换词（2026-09-24）

「哇，你终于回来了～」→ **欢迎回港，时时更新的忒修斯之船。**（`HomePage.astro` 的 `.home-welcome-bubble`；下半区那句说明仍是第 41 条的「这里会落下一些我路过的时代，它们总是早盛早衰」）

验证：`npm test` 17/17；dev 首页 HTML 里已是新文案、旧文案不存在；掉落标签仍是第 41 条那 16 个。

## 45. 清空项目卡片（2026-09-24）

用户：「把所有的卡片删掉」——按「Projects 页那 6 张示例卡片」处理（那是上一轮我从预览页搬过去的占位数据）。

- `src/core/data/projects.ts` 的 `projectEntries` 清空，只留类型定义、字段注释和一条注释掉的示例；`projectTechLines` 也保持空数组。
- 拼贴墙的渲染逻辑、详情面板、样式、脚本全部保留：**往这个文件里加条目，卡片就会回来**，不用改页面。数据为空时 `/projects/` 走原来的空状态文案（「这里还在等第一张卡片…」）。
- Blank 主题的 Projects 页同样跟着变空（它读同一份数据）。

验证：`npm test` 17/17；`npm run build` 16 页 + 预算全过；dist 与 dev 的 `/projects/` 里卡片按钮数为 **0**、没有详情 overlay、空状态文案在（HTML 里剩下的 `.mosaic-card` 字样都是 CSS 选择器）。

> 备注：`/preview/projects/` 这页还留着自己写死的 6 张示例（它是设计画廊，noindex + sitemap 排除）。等用户确认删预览页时一并清掉。

## 46. 右下角胶囊上移一格滚轮（2026-09-24）

用户：「把自动下拉胶囊的位置再上移一个滚轮」——按「右下角悬停会自动拉出来的快捷胶囊」处理。

- Fuyukawa：`.quick-actions` 的 `bottom` 20px → **80px**；≤940px 那档 18px → **78px**。
- Blank：`bottom` 22px → **82px**（保持两个主题同一高度）。

验证：`npm test` 17/17；`npm run build` 16 页 + 预算全过；dev 首页内联 CSS 里已是 `bottom: 80px`。

> 备注：如果用户指的是英雄区底部那个 `scroll@home:~$` 下拉提示（`.scroll-cue`，`bottom: 18px`），改的是那条，需要下一轮再动。

## 47. 顶部自动下拉导航：收起位置上移一个滚轮（2026-09-25）

第 46 条改错了对象（用户说的是「上面那个」）。用无头浏览器量过：`.site-header` 收起在 `top: -78px`、盒子高 96px，于是**底部 18px 露在视口里**；它的 `::before` 那颗 46×6、`border-radius: 999px` 的粉色把手胶囊落在 **y ≈ 26–32px**，悬停它会把导航拉下来——这就是用户看到的「自动下拉胶囊」。

- `.site-header` 的收起位置 `top: -78px` → **-138px**（再上一个滚轮格 ≈ 60px）；实测把手胶囊 `pillVisibleTop` 从 26 变成 **-56**，导航底边也不再露头。
- 因为收起后整条 header 都在视口外，新增 `.nav-hotzone`（`position: fixed; top: 0; height: 14px; z-index: 19`，比 header 低一层，展开时不挡点击）+ `body:has(.nav-hotzone:hover) .site-header { top: 0 }`，鼠标扫到页面最上方照样能把导航拉下来。
- 同时撤回第 46 条改错的右下角胶囊位置（Fuyukawa 回到 `bottom: 20px`、窄屏 18px；Blank 回到 22px）。

验证：`npm test` 17/17；`npm run build` 16 页 + 预算全过；无头浏览器复测 header `top: -138px`、`headerPinned: false`、把手胶囊已移出视口。

## 48. 顶部导航「取消隐藏的位置」上移（2026-09-25）

第 47 条又理解错了：用户要的是**取消隐藏的触发位置**上移，不是把 header 收得更远。已全部撤回并只改门槛：

- `.site-header` 的收起位置改回 `top: -78px`（把手小条与收起状态恢复原样，`nav-hotzone` 元素与样式一并删除）。
- `HomePage.astro` 的 `syncNavPin`：`homeContentBg.getBoundingClientRect().top <= 0` → **`<= 60`**，即下半区还差一个滚轮格（约 60px）到顶时，顶部那条胶囊就取消隐藏并常驻。
- 测试断言同步改成 `top <= 60`。

验证：`npm test` 17/17；dev 首页内联 CSS 已是 `top: -78px`，脚本里门槛为 `top <= 60`。

## 49. 公开前的清理一批（2026-09-25）

用户「grill me → 按你推荐的来 → 一次性做了」授权后执行，Q7（站点身份）等仓库信息：

1. **tmp 临时产物**：删掉 5 张验证截图（≈4.6 MB）；保留 4 个 `verify-*.mjs`、`bangumi-raw.json`、`tmp/friends`（3.3 MB）与 `tmp/icons`（2 MB）素材。
2. **音乐版权**：查清 `MUSIC/` 里 13 首是用户自己 09-14~09-24 添加的（就是歌单），`public/…/music/` 里另有 9 个 09-02 的原站遗留（不在 manifest、却会随构建发布）→ 只删那 9 个；重新构建后 `dist` 只剩 13 首 + manifest，`MUSIC/` 一个字节没动。
3. **腾讯地图 key**：`HomePage.astro` 里 `tencentMapKey = "PDHBZ-…"` 与 `apis.map.qq.com` 的 JSONP 调用整段删除，`getIpLocation` 直接走 `ipwho.is`（天气仍 `open-meteo`，失败有时区兜底）；测试断言从「必须调用」改成「不得再出现」。
4. **第三方说明**：README 里 Live2D 那段补上「启用会请求 `fastly.jsdelivr.net` / `cubism.live2d.com`，模型版权归 Live2D 与原作者、未随站分发」。
5. **删预览页与收尾**：删除 `src/pages/preview/reader.astro`、`src/pages/preview/projects.astro`；`astro.config.mjs` 的 sitemap 过滤去掉 `/preview/`；`BaseLayout` 的 `noindex` 属性与对应 meta 收掉（主题备用路由的 `noindex,follow` 保留）；测试里盯预览页的断言整段重写成盯共享文件。

验证：`npm test` 17/17；`npm run build` 16 页 + 预算全过（About CSS 119.9 KiB）。

**仍未做**：`theme.css` 里 `.article-toc*`（约 60 行，已确认无人引用）与项目页旧 `.works-*` 的摘除——`.works-shell / .works-hero / .works-empty` 还在用，得分清哪些是死样式再删，下一轮单独处理。

**Q7 站点身份（已定，2026-09-25）**：`astro.config.mjs` 的 `site` 已改为 `https://animaigo.github.io`，部署到 GitHub Pages 用户主页仓库 `Animaigo/Animaigo.github.io`（根路径、无需 `base`），线上 `https://animaigo.github.io/`。`edgeone.json` 作为历史配置保留、当前不生效；`preview-blog.bat` 是本地预览脚本、仍可用。

## 26. Anime 共享元素过渡：文字平移放大成标题（2026-09-19）

门槛判定：用户主动点击导航（偶发），目的是 **spatial consistency**——让标题看得出"从 Me 页那个入口长出来"，而不是凭空出现。

实现方式（两个主题走不同机制，效果一致）：

- **Fuyukawa**（已有 `ClientRouter`）：Me 页的 `<span class="me-more-link-label">Anime</span>` 与 `/anime/` 的 `<h1>Anime</h1>` 都加 `transition:name="anime-title"`，由 Astro 的共享元素配对触发浏览器原生 group morph（位置 + 尺寸插值）；Astro 自带 180ms 旧/新快照交叉淡入。
- **Blank**（无客户端路由、整页加载）：在 `blank/styles/theme.css` 启用跨文档 View Transition —— `@view-transition { navigation: auto; }`，并给两个元素加 `view-transition-name: anime-title`（About 侧的 `.blank-link-row-label`、Anime 侧的 `.blank-anime-title`）。同时用 `::view-transition-old(root), ::view-transition-new(root) { animation: none; }` 关掉整页淡入，只保留 Anime 那个词的位移动画，避免给所有 Blank 内跳转都加上页面过渡。
- 缓动与时长：`::view-transition-group(anime-title)` 统一为 `320ms cubic-bezier(0.77, 0, 0.175, 1)`（即技能的 `--ease-in-out`，页面级过渡取 modal/drawer 的 200–500ms 档）；只动 transform/opacity，由浏览器合成层完成。
- Reduced motion：Fuyukawa 由 Astro 自带的 `@media (prefers-reduced-motion)` 保护；Blank 侧显式补了 `::view-transition-group(*)/old(*)/new(*) { animation: none !important }`。

改动位置：`src/themes/fuyukawa-kagari/pages/AboutPage.astro`、`src/themes/fuyukawa-kagari/pages/AnimePage.astro`、`src/themes/fuyukawa-kagari/styles/theme.css`、`src/themes/blank/pages/AnimePage.astro`、`src/themes/blank/styles/theme.css`。

验证：`npm test` 14/14 通过；`npm run build` 14 页与性能预算全部通过；dist 与 dev 均确认两页都输出 `view-transition-name: anime-title`、Fuyukawa 侧有 `data-astro-transition-scope` 作用域、Blank 侧有 `@view-transition{navigation:auto}` 与 group 时长规则。动画手感（位移轨迹、交叉淡入）仍需目视验收——需实际点击链接，直接刷新地址栏不会触发。
